# incident-log.md（失敗記録台帳・横断）

- 対象: プロジェクトに属さない作業（横断ルール整備・`00_Template` 保守等）
- 運用: 失敗・指摘の1回目をここに記録する。2回目でルール昇格を提案する（`CROSS_PROJECT_RULES.md` のメタルール参照）
- 状態: `様子見`（1回目）/ `昇格済`（ルール化。対処先を `昇格済（CR-085・機械化は B-11）` のように付記してよい）/ `再発（未対処）`（ルール化後も発生し、まだ対処していない）/ `再発（対処: CR-054 改訂）`（再発後に対処した。根拠の要件は CR-017）

| 日付 | 事象 | 原因 | 暫定対処 | 状態 | 関連ルール |
|------|------|------|----------|------|------------|
| 2026-07-13 | 生成物の日付を 2026-07-12 と誤記（実際は 07-13） | env の現在日付を確認せず記載 | 02_Roles 全13ファイルと横断ルール追記分を 07-13 に修正 | 様子見 | - |
| 2026-07-13 | ドキュメントに全角スペースを使用 | 半角/全角の区別を意識せず記述 | 全ファイルから除去し作業ルールへ追記 | 昇格済 | CR-024（空白は半角スペース） |
| 2026-07-14 | 変更なし確認で `git status` を実行（当時の「Git 操作はしない」に抵触） | 変更確認に git を使用。サンドボックスの unlink 不可（EPERM）で `.git\index.lock` が残留しかけた | lock を rename で除去。CR-061 は 2026-07-16 に読取専用 Git を許可する内容へ改訂 | 様子見 | CR-061（改訂前） |
| 2026-07-14 | オーケストレーション運用策定時にモデル能力順を誤断定（Opus 最上位・Fable 創作限定） | 学習範囲外の新モデル情報を検証せず名称から役割を推測して断定 | 公式 Models overview で Fable 5 最上位を確認し訂正。役割ティア方式へ変更し固有名と能力順を対応表に隔離、能力順は依頼者確認 | 様子見 | OR-003・OR-004（役割ティア・対応表） |
| 2026-07-14 | ホスト編集直後のファイルがサンドボックスで旧サイズのまま途切れて見える事象が同一セッションで4ファイルに発生（伸びた分だけ末尾欠落） | マウントのサイズキャッシュ固着 | 検証はローカル実行を正ᴳとし、復旧は新規パスへの書出しで回避 | 昇格済 | CS-002・CS-004 |
| 2026-07-16 | 独立工程が複数あるタスクᴳでもサブエージェント委譲・並列が発火せず、上位ティア主が直列処理していた（直近セッション精査で委譲0件） | OR-001 の「単純作業は直接処理」既定と使用量残の非可視が委譲を抑制。並列の実行条件（同一ターン起動）が散文で埋もれ行動として効かず | 委譲条件と並列実行を明文化。2026-07-16 の再棚卸しで、品質優先のため一律委譲から「独立並列・専門性分離・品質ゲート監査」の条件付き委譲へ改訂 | 昇格済 | OR-008・OR-009・OR-010・OR-011・OR-012・OR-001・OR-005 |
| 2026-07-31 | B-10 の移行判定で、FF14 台帳の CR-069 の再発 9 件を「CR-085 / CR-086 / CR-087 の新設で対処済み」と誤判定した。フェーズ 2 の独立監査が「楽観的分類でベースラインが汚染される」と事前に警告していた筋書きに、警告を受けたうえで踏み込んだ | 新設日が再発日より後であることだけを根拠にし、そのルールが「どの事象に対して」作られたかを台帳で確認しなかった。CR-085 等が対処したのは 07-25〜07-30 の事象で、既に `昇格済` として記録済みだった | フェーズ 4 の独立監査と実測で捕捉し、判定を撤回。CR-017 へ「対処済みへ移してよいのは再発日より後の改訂・新設・機械化を `file:line` で示せる場合に限る」を明記。2026-09-13: CR-017を改訂し、当該事象の原因との対応・実施時点・根拠の確認を必須化（CROSS_PROJECT_RULES.md:31）。日付だけで無関係な対策へ帰属させた原因に対応 | 再発（対処: CR-017 改訂） | CR-069・CR-084 |
| 2026-07-31 | B-10 の移行判定をルール単位で行い、状態欄が行単位でしか値を持てないというデータモデルとの整合を確認しなかった。1 行に `CR-025・CR-069` が同居する 2 行へ、1 つのセルでは表現できない判定を割り当てていた | 判定の粒度と、判定結果を書き込む先の構造を突き合わせなかった | フェーズ 4 の独立監査で捕捉。判定粒度を行（事象）単位へ統一し、要件・設計とも行番号単位の一覧へ書き直した。2026-09-13: CR-017を改訂し、事象行単位の判定と一部原因のみ対処した行の未対処維持を明記（CROSS_PROJECT_RULES.md:31）。判定粒度と台帳構造の不一致に対応 | 再発（対処: CR-017 改訂） | CR-069 |
| 2026-07-31 | B-10 の設計でセル内の縦棒をエスケープする規約を決めたが、その書式を読むパーサ側の対応を設計に含めず、台帳を移行しても書式異常が消えなかった | 書式規約の変更を「書く側」の問題としてのみ捉え、「読む側」の対応を同一の変更範囲に含めなかった | 実装後の検算で発覚。パーサをエスケープ対応へ修正し、回帰テストを追加。CR-088 を新設 | 昇格済（CR-088） | CR-088 |
| 2026-07-31 | 読み取り禁止のプロジェクトがある環境で、全プロジェクトを再帰走査する既定実行を独立監査者に行わせ、当該プロジェクトの台帳内容が出力へ漏れた | 追加した検査が全プロジェクトの台帳を対象にすることを認識しながら、監査の委譲指示で対象範囲を限定しなかった | 監査者が即時報告し以降踏み込まず。委譲時は `--no-recursive` または対象ディレクトリの明示を指示する | 様子見 | - |
| 2026-09-09 | 終了記録の整備だけで追加commit・push・mergeが必要になり、依頼者から工程集約を求められた | 反映前成果物と反映後の成功証跡を分離せず、例外経路も完了記録の再コミットを要求していた | CR-049とsession-phases.mdを改訂し、文書・振り返りをPR前に集約。終了手続きでは対象リポジトリを編集せず、merge・CI・実機結果は外部へ記録。CI等で修正が必要な場合は該当工程へ戻す | 昇格済（CR-049） | CR-049 |
