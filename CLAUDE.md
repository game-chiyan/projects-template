# CLAUDE.md（Claude 入口）

- 対象: `~\Projects` 配下で Claude が作業する全セッション
- 位置づけ: 本書 = Claude 向け入口（常時読み込むコア）。プロジェクト横断ルールの実体は `CROSS_PROJECT_RULES.md`、状況別の詳細ルールは `~\Projects\04_Rules_Reference\` に置く
- 読み順: 本書 → `CROSS_PROJECT_RULES.md`。特定プロジェクトが対象の場合だけ `PROJECT_RULES.md` → `resume.md`（あれば）を読み、handoverᴳ / `worklog.md` は詳細確認が必要なときだけ参照する
- 状況別の参照先は `~\Projects\04_Rules_Reference\README.md` の参照トリガーに従う
- 優先順位は `CROSS_PROJECT_RULES.md` 冒頭に従う

## セッション開始チェックリスト（実作業の前に順に完了させる）

1. 読み順に従い、対象が特定プロジェクトの場合だけ状態を把握する
2. ブランチを用いる開発タスクᴳでは、フェーズᴳ 0 の合意後に CR-060 のブランチ名・チャット名を提示する
3. 規範索引の参照トリガーに該当する状況があれば、該当ファイルを先に読む
4. 独立工程の並列化、異なる専門性の分離、品質ゲートでの独立監査が有効な場合はサブエージェントを使う。詳細は `~\Projects\04_Rules_Reference\orchestration.md` に従う

## Claude 固有ルール

- CL-004: 選択肢を提示する前に実行可能性を検証し、仕様上一方しか選べない場合は単一案として確認する
- CL-005: ドキュメント内の第三者名・過去の担当者名を依頼者本人と推定せず、依頼者への名前での呼びかけは行わない
- CL-011: 不可視文字・特殊文字を含む行は照合とコードポイントを確認できる方法で編集し、書込み後に実体文字を検証する
- CL-012: セッション開始時に Projects MCP の許可ディレクトリが `uploads` のみ（実コード未マウント）の場合、実作業の前に `request_cowork_directory` でフォルダ選択ダイアログを提示し、`~\Projects` へのアクセス許可を求める

## CLAUDE.md を育てるルール（メタルール）

1. CL-006: 追記トリガー: Claude 固有の失敗・制約・有効な回避策を発見した場合に追記候補とする
2. CL-007: 分析様式は CR-002、本文の簡潔さは CR-010 に従う
3. CL-008: 配置判定: Claude 固有かつ常時必要な行動ルールは本書、状況限定の詳細は `04_Rules_Reference\` の該当ファイル、全AIツール共通なら `CROSS_PROJECT_RULES.md`、特定プロジェクトだけなら `PROJECT_RULES.md` に置く。参照トリガーは規範索引へ集約する
4. CL-009: 更新フローは CR-005、採番は CR-013 に従う
5. CL-010: 肥大化抑制: 本書は入口・チェックリスト・常時必要な固有ルールに限定し、共通ルール・規範索引・状況別詳細を重複記載しない
